name: sync-to-cnb
on:
    - push
    - delete

jobs:
    call-cnb-sync:
        uses: TDesignOteam/workflows/.github/workflows/reusable-cnb-sync.yml@50ba4f86432c1145d41342166190c973efbc337c # main
        with:
            github-repo: TDesignOteam/tdesign-api
            cnb-repo: tencent/tdesign/tdesign-api
        secrets:
            CNB_TOKEN: ${{ secrets.CNB_TOKEN }}
